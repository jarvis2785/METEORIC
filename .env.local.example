# Make.com webhook that receives application form submissions as JSON.
# Copy this file to .env.local and paste the real webhook URL.
# If unset, submissions are logged to the browser console and the UI still
# shows the success state (useful for demos before the automation is wired).
NEXT_PUBLIC_MAKE_WEBHOOK_URL=https://hook.make.com/your-webhook-id
